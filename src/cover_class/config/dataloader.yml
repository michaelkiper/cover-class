version: v0
tags: initial-config classes[]
drop-bands-wavelengths: # needs to be a list of tuples
  - [5, 5] # for one-band items
  - [10, 200]
datasets:
  soil:
    - /some/path.hdf5
    - /some/path.hdf5
  pv:
    - /some/path.hdf5
  npv:
  ash+char:
  snow+ice:
  water:
generalization-test-set: /some/path/to/generalization-test-set.hdf5
simulation:
  n_components: [1,5]
  n_classes_in_subsets: 3
  alpha: 0.3
  min_frac: 0.1
  alpha_uniform_low: 0.1
  alpha_uniform_high: 10.
  white_noise: 0.0001
  noise_covariance_csv: example.csv
  return_fractions: false
subsample:
  test-fraction: 0.2
  selected-method: convex-hull
  convex-hull:
    num_pc: 1
    n_samples: 10
    qhull_options:
  kmeans:
    num_pc: 1
    n_samples: 10
    init:
    n_init:
    max_iter:
    tol:
    random_state:
    algorithm:
  kmedoids:
    num_pc: 1
    n_samples: 10
    metric:
    method:
    init:
    max_iter:
    random_state:
    metric: mahalanobis
  lhs:
    num_pc: 1
    n_samples: 10
    hypercubes_per_dimension: 3
    samples_per_hypercube: 3
dataloader:
  percent-static-data: 0.5
